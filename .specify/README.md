# SpecKit workspace

This directory is reserved for the actual SpecKit-generated lifecycle metadata. Run `specify init . --integration claude` in the assessment environment and preserve the generated files. Do not fabricate SpecKit output.
