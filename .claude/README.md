# Claude Code integration

Run SpecKit initialization with `--integration claude` to generate the authoritative Claude skills in this directory. Do not fabricate generated skills or session evidence.
