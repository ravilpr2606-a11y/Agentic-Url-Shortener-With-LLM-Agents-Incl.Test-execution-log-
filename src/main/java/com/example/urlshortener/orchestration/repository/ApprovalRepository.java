package com.example.urlshortener.orchestration.repository; import com.example.urlshortener.orchestration.domain.Approval; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface ApprovalRepository extends JpaRepository<Approval,UUID>{ Optional<Approval> findTopByRunIdAndGateOrderByDecidedAtDesc(UUID runId,String gate); List<Approval> findByRunIdOrderByDecidedAtAsc(UUID runId); }
